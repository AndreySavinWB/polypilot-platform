# Схема данных события PP

> Минимальный контракт между backend, агентами и HTML-продуктом.

---

## Event Input

```json
{
  "id": "string",
  "slug": "string",
  "title": "string",
  "description": "string",
  "category": "string",
  "volume": "number|string",
  "volume24hr": "number|string",
  "liquidity": "number|string",
  "startDate": "string",
  "endDate": "string",
  "marketsCount": 1,
  "markets": [
    {
      "id": "string",
      "question": "string",
      "conditionId": "string",
      "clobTokenIds": "string|array",
      "outcomes": "string|array",
      "outcomePrices": "string|array",
      "volume": "number|string",
      "liquidity": "number|string"
    }
  ],
  "source": "polymarket_gamma",
  "sourceUrl": "string"
}
```

---

## Agent Analysis Output

```json
{
  "eventId": "string",
  "priority": {
    "agent": "Priority Agent",
    "score": 75,
    "decision": "accepted|watchlist|rejected",
    "reason": "string"
  },
  "newsScout": {
    "agent": "News Scout",
    "summary": "string",
    "facts": ["string"],
    "missing": ["string"]
  },
  "riskOfficer": {
    "agent": "Risk Officer",
    "riskLevel": "low|medium|high",
    "flags": ["string"]
  },
  "verdict": {
    "agent": "Verdict Agent",
    "ppVerdict": "string",
    "confidence": 42,
    "edgeScore": null,
    "status": "research_required|ready|rejected"
  },
  "ui": {
    "closedCardBadge": "string",
    "warRoomAgents": ["string"],
    "disclaimer": "string"
  }
}
```

---

## Почему строгий JSON

- HTML-MVP сможет отрисовать карточку из backend.
- Можно сравнивать версии анализа.
- Можно тестировать агентов по scorecard.
- Можно сохранять историю для Proof Track и Autopsy.

---

Связанные:
- [[ПРОСТОЙ_BACKEND]]
- [[ЛАБОРАТОРИЯ_АГЕНТОВ]]
- [[AI_КАРТОЧКА_СОБЫТИЯ]]
